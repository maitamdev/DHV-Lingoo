export function generateId(len = 8) { return Math.random().toString(36).substring(2, 2 + len); }
// Crypto collision resist
// Achievement ID generation
// Practice session IDs for analytics tracking
