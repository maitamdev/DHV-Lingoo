'use client';import{useState}from'react';export default function PasswordInput({value,onChange,placeholder='Password'}:{value:string;onChange:(v:string)=>void;placeholder?:string}){const[show,s]=useState(false);return(<div className='relative'><input type={show?'text':'password'} value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className='w-full px-3 py-2 border focus:border-blue-500 outline-none pr-12'/><button type='button' onClick={()=>s(!show)} className='absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm'>{show?'Hide':'Show'}</button></div>)}
