export default function TextArea({value,onChange,placeholder='',rows=4,className=''}:{value:string;onChange:(v:string)=>void;placeholder?:string;rows?:number;className?:string}){return(<textarea value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} rows={rows} className={'w-full px-3 py-2 border focus:border-blue-500 outline-none resize-y '+className}/>)}
