'use client';import{useState}from'react';export default function TabBar({tabs,className=''}:{tabs:{label:string;content:React.ReactNode}[];className?:string}){const[a,s]=useState(0);return(<div className={className}><div className='flex border-b'>{tabs.map((t,i)=>(<button key={i} onClick={()=>s(i)} className={'px-4 py-2 text-sm font-semibold border-b-2 '+(i===a?'border-blue-500 text-blue-600':'border-transparent text-gray-500')}>{t.label}</button>))}</div><div className='pt-4'>{tabs[a]?.content}</div></div>)}
