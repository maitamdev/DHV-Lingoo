'use client';import{useEffect,useState}from'react';export default function Countdown({targetDate}:{targetDate:string}){const[t,s]=useState('');useEffect(()=>{const i=setInterval(()=>{const d=new Date(targetDate).getTime()-Date.now();if(d<=0){s('Ended');clearInterval(i);return}const h=Math.floor(d/3600000);const m=Math.floor((d%3600000)/60000);const sc=Math.floor((d%60000)/1000);s(h+'h '+m+'m '+sc+'s')},1000);return()=>clearInterval(i)},[targetDate]);return(<span className='font-mono font-bold'>{t}</span>)}
